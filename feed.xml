<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="https://satgoy152.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://satgoy152.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-11-10T07:39:48+00:00</updated><id>https://satgoy152.github.io/feed.xml</id><title type="html">blank</title><subtitle>Website for Satyam Goyal </subtitle><entry><title type="html"></title><link href="https://satgoy152.github.io/blog/2025/2025-11-10-sedd/" rel="alternate" type="text/html" title=""/><published>2025-11-10T07:39:48+00:00</published><updated>2025-11-10T07:39:48+00:00</updated><id>https://satgoy152.github.io/blog/2025/2025-11-10-sedd</id><content type="html" xml:base="https://satgoy152.github.io/blog/2025/2025-11-10-sedd/"><![CDATA[<h1 id="understanding-sedd-a-theoretically-grounded-approach-to-discrete-diffusion-for-language">Understanding SEDD: A Theoretically Grounded Approach to Discrete Diffusion for Language</h1> <p>Diffusion models have revolutionized image generation, but their application to language has been far less successful. While autoregressive models like GPT continue to dominate text generation, there’s a compelling reason to explore alternatives: diffusion-based language models can leverage bidirectional attention and enable more flexible generation patterns like infilling and controllable prompting from arbitrary positions.</p> <p>I’m currently working with Professor JJ Park at the University of Michigan on improving Masked Discrete Diffusion Models (MDLMs), which motivated me to revisit a 2024 paper that won best paper at ICML: “Discrete Diffusion Modeling by Estimating the Ratios of the Data Distribution” by Aaron Lou, Chenlin Meng, and Stefano Ermon at Stanford. In my view, this is one of the most theoretically grounded papers explaining why and how discrete diffusion can be an effective strategy for language generation.</p> <p>This blog aims to make the core concepts accessible, but I encourage reading the <a href="https://arxiv.org/pdf/2310.16834">full paper</a>, Aaron Lou’s excellent <a href="https://aaronlou.com/blog/2024/discrete-diffusion/">blog post</a>, and his <a href="https://www.youtube.com/watch?v=_1qv_LNjH9U">presentation</a> for deeper understanding.</p> <h2 id="the-problem-with-discrete-diffusion-for-language">The Problem with Discrete Diffusion for Language</h2> <p>Most masked diffusion models for language follow a straightforward pattern: during forward diffusion, tokens from the original text are progressively replaced with MASK tokens. During reverse diffusion, the model learns to predict the original tokens at each masked position. You directly sample tokens from the model’s output distribution at each position in the context window.</p> <p>This approach seems natural, but it has struggled to match autoregressive models in practice. The SEDD paper identifies a fundamental issue: existing training objectives for discrete diffusion don’t properly capture the structure of discrete probability distributions.</p> <h2 id="the-key-insight-modeling-probability-ratios">The Key Insight: Modeling Probability Ratios</h2> <p>Here’s where SEDD diverges from prior work. Instead of predicting tokens directly, the model learns to predict ratio scores:</p> \[s_\theta(x, t)[i, j] \approx \frac{p_t(\text{token } j \text{ at position } i)}{p_t(\text{current token at position } i)}\] <p>This ratio tells us how likely we are to transition from the current token at position $i$ to any other token $j$ in the vocabulary. Why is this better? These ratios are the discrete analog of the score function $\nabla_x \log p_t(x)$ used in continuous diffusion models, giving us a theoretically principled connection to the well-established score matching framework.</p> <h2 id="forward-diffusion-with-transition-matrices">Forward Diffusion with Transition Matrices</h2> <p>To understand how SEDD works, we need to look at how the forward diffusion process corrupts the data. The process is governed by a continuous-time Markov chain defined by a transition rate matrix $Q_t$:</p> \[\frac{dp_t}{dt} = Q_t p_t, \quad p_0 \approx p_{\text{data}}\] <p>The matrix $Q_t$ has non-negative off-diagonal entries and columns that sum to zero, ensuring the total probability mass is conserved. The paper explores two specific transition structures:</p> <p><strong>Uniform Transition</strong>: Every token can transition to any other token with equal probability. The transition matrix looks like:</p> \[Q_{\text{uniform}} = \begin{bmatrix} 1-N &amp; 1 &amp; \cdots &amp; 1 \\ 1 &amp; 1-N &amp; \cdots &amp; 1 \\ \vdots &amp; \vdots &amp; \ddots &amp; \vdots \\ 1 &amp; 1 &amp; \cdots &amp; 1-N \end{bmatrix}\] <p>where $N$ is the vocabulary size.</p> <p><strong>Absorbing Transition</strong>: Tokens can only transition to a special MASK token, which acts as an absorbing state:</p> \[Q_{\text{absorb}} = \begin{bmatrix} -1 &amp; 0 &amp; \cdots &amp; 0 &amp; 0 \\ 0 &amp; -1 &amp; \cdots &amp; 0 &amp; 0 \\ \vdots &amp; \vdots &amp; \ddots &amp; \vdots &amp; \vdots \\ 0 &amp; 0 &amp; \cdots &amp; -1 &amp; 0 \\ 1 &amp; 1 &amp; \cdots &amp; 1 &amp; 0 \end{bmatrix}\] <p>The absorbing transition is particularly interesting because it mirrors BERT-style masking, which has proven effective for language understanding tasks.</p> <p>In practice, we scale these matrices by a noise schedule $\sigma(t)$, so $Q_t = \sigma(t) Q$. This allows us to control how quickly the corruption happens. The forward transition probabilities can be computed analytically:</p> \[p_{t|0}^{\text{tok}}(\cdot|x) = \text{column } x \text{ of } \exp(\sigma(t) Q^{\text{tok}})\] <p>This means at each timestep, tokens transition independently according to this probability distribution.</p> <h2 id="score-entropy-the-main-contribution">Score Entropy: The Main Contribution</h2> <p>The paper’s key theoretical contribution is the score entropy loss. Previous attempts to adapt score matching to discrete spaces struggled because they didn’t properly handle the constraint that probability ratios must be positive. Concrete score matching, for instance, uses an $\ell_2$ loss that doesn’t penalize negative predictions enough, leading to training instabilities.</p> <p>Score entropy is defined as:</p> \[L_{SE} = \mathbb{E}_{x \sim p} \left[ \sum_{y \neq x} w_{xy} \left( s_\theta(x)_y - \frac{p(y)}{p(x)} \log s_\theta(x)_y + K\left(\frac{p(y)}{p(x)}\right) \right) \right]\] <p>where $K(a) = a(\log a - 1)$ is a normalizing constant that ensures $L_{SE} \geq 0$.</p> <p>This loss is built on a Bregman divergence rather than the Fisher divergence used in standard score matching. Why does this matter? The score entropy loss naturally keeps the predicted ratios positive through a log-barrier effect. When you compute the gradient with respect to $s_\theta(x)<em>y$, you get a $\frac{1}{s</em>\theta(x)_y}$ scaling factor that grows large as predictions approach zero, preventing the network from outputting negative or zero values.</p> <p>The beauty of score entropy is that it generalizes cross-entropy to arbitrary positive values, not just probability distributions. It’s non-negative, symmetric, and convex, making it a proper loss function. Most importantly, when you minimize it with enough data and model capacity, you provably recover the true probability ratios.</p> <h2 id="making-score-entropy-tractable">Making Score Entropy Tractable</h2> <p>The score entropy loss as written contains unknown ground truth ratios $\frac{p(y)}{p(x)}$. To make this practical, the paper derives a denoising variant analogous to denoising score matching in continuous diffusion:</p> \[L_{DSE} = \mathbb{E}_{x_0 \sim p_0} \mathbb{E}_{x \sim p(\cdot|x_0)} \left[ \sum_{y \neq x} w_{xy} \left( s_\theta(x)_y - \frac{p(y|x_0)}{p(x|x_0)} \log s_\theta(x)_y \right) \right]\] <table> <tbody> <tr> <td>This is computationally efficient because we only need to evaluate $s_\theta(x)$ once per sample (giving us all ratios for that position), and the conditional transition probabilities $\frac{p(y</td> <td>x_0)}{p(x</td> <td>x_0)}$ are known from our forward process.</td> </tr> </tbody> </table> <p>For training, the full diffusion-weighted denoising score entropy integrates this loss over time:</p> \[L_{DWDSE}(x_0) = \int_0^T \mathbb{E}_{x_t \sim p_{t|0}(\cdot|x_0)} \left[ \sum_{y \neq x_t} Q_t(x_t, y) \left( s_\theta(x_t, t)_y - \frac{p_{t|0}(y|x_0)}{p_{t|0}(x_t|x_0)} \log s_\theta(x_t, t)_y + K\left(\frac{p_{t|0}(y|x_0)}{p_{t|0}(x_t|x_0)}\right) \right) \right] dt\] <p>The paper proves this provides an upper bound on the negative log-likelihood, allowing for principled likelihood-based evaluation and training.</p> <h2 id="reverse-diffusion-and-sampling">Reverse Diffusion and Sampling</h2> <p>Once we’ve learned the score function $s_\theta(x, t)$, we can define the reverse diffusion process. The reverse transition rates are given by:</p> \[\tilde{Q}_t(y, x) = \frac{p_t(y)}{p_t(x)} Q_t(x, y)\] <p>Since we’ve learned the ratios $s_\theta(x, t)_y \approx \frac{p_t(y)}{p_t(x)}$, we can construct an approximate reverse process:</p> \[\tilde{Q}_t^\theta(y, x) = s_\theta(x, t)_y Q_t(x, y)\] <p>The reverse process follows:</p> \[\frac{dp_{T-t}^{\theta}}{dt} = \tilde{Q}_{T-t}^\theta p_{T-t}^{\theta}, \quad p_T^\theta = p_{\text{base}} \approx p_T\] <h2 id="a-concrete-example-forward-and-reverse-diffusion">A Concrete Example: Forward and Reverse Diffusion</h2> <p>Let’s walk through a simplified example using absorbing transitions with the sentence “cat in hat” (3 tokens). Suppose our vocabulary is {cat, in, hat, dog, the, MASK} with size $N=6$.</p> <p><strong>Forward Diffusion Example:</strong></p> <p>Starting with $x_0 = [\text{cat}, \text{in}, \text{hat}]$ at $t=0$, we apply the absorbing transition. Let’s say we’re at $t=0.5$ with noise schedule giving $\sigma(0.5) = 1.5$.</p> <p>The transition probability from “cat” to MASK is given by the exponential of the scaled Q matrix:</p> \[p_{0.5|0}(\text{MASK}|\text{cat}) = [\exp(1.5 \cdot Q_{\text{absorb}})]_{\text{cat}, \text{MASK}} \approx 0.78\] <p>After sampling, we might get $x_{0.5} = [\text{MASK}, \text{in}, \text{MASK}]$. Each token transitions independently, so “in” happened to stay unchanged while “cat” and “hat” both transitioned to MASK.</p> <p><strong>Reverse Diffusion Example:</strong></p> <p>Now let’s reverse from $x_{0.5} = [\text{MASK}, \text{in}, \text{MASK}]$ back toward $t=0$.</p> <p>At position 0 (currently MASK), our model predicts scores:</p> <ul> <li>$s_\theta(x_{0.5}, 0.5)[0, \text{cat}] = 4.2$</li> <li>$s_\theta(x_{0.5}, 0.5)[0, \text{dog}] = 0.8$</li> <li>$s_\theta(x_{0.5}, 0.5)[0, \text{the}] = 1.1$</li> <li>$s_\theta(x_{0.5}, 0.5)[0, \text{in}] = 0.3$</li> <li>$s_\theta(x_{0.5}, 0.5)[0, \text{hat}] = 0.5$</li> </ul> <p>These scores represent the ratio $\frac{p_{0.5}(\text{token } j \text{ at position } 0)}{p_{0.5}(\text{MASK at position } 0)}$.</p> <p>To sample the reverse step using Tweedie denoising (the paper’s recommended strategy), we compute:</p> \[p(x_0^{(0)} | x_{0.5}) \propto [\exp(-\sigma_{0.5}^{0.5} Q) \cdot s_\theta(x_{0.5}, 0.5)[0]]_{x_0^{(0)}} \cdot \exp(\sigma_{0.5}^{0.5} Q)(\text{MASK}, x_0^{(0)})\] <p>where $\sigma_{0.5}^{0.5} = \sigma(0.5) - \sigma(0)$ is the cumulative noise difference.</p> <p>This computation combines our learned scores with the known forward transition structure to optimally denoise. With our scores above, “cat” has the highest ratio, so it’s most likely to be sampled at position 0.</p> <p>After applying this at each position, we might get $x_0 = [\text{cat}, \text{in}, \text{hat}]$, successfully recovering the original text.</p> <p>The key insight is that by modeling ratios rather than direct probabilities, we can compose the reverse transitions in a theoretically principled way that leverages the structure of the forward process.</p> <h2 id="why-this-works-better">Why This Works Better</h2> <p>The combination of score entropy training and ratio-based parameterization leads to substantial improvements:</p> <p>On GPT-2 zero-shot tasks, SEDD Absorb beats GPT-2 on the majority of benchmarks for perplexity. On the One Billion Words dataset, it reduces perplexity by 50-75% compared to prior diffusion models. For unconditional generation quality (measured by generative perplexity), SEDD produces 6-8× better samples than unannealed GPT-2 and can match GPT-2 quality using 32× fewer function evaluations.</p> <p>Perhaps most impressively, SEDD enables controllable infilling without specialized training. Because the model learns probability ratios, you can condition on arbitrary positions through Bayes’ rule. For text infilling tasks, SEDD matches nucleus sampling quality from autoregressive models while supporting more flexible prompting strategies.</p> <h2 id="closing-thoughts">Closing Thoughts</h2> <p>The SEDD paper demonstrates that with the right theoretical foundation, discrete diffusion can match or exceed autoregressive models for language generation. The score entropy loss solves a fundamental problem in adapting score matching to discrete spaces, providing a principled training objective that actually works in practice.</p> <p>What excites me most about this work is how it opens up new possibilities for language modeling. Bidirectional attention, parallel generation, and flexible conditioning are all natural in the diffusion framework but difficult to achieve with autoregressive models. As we continue improving masked discrete diffusion models, papers like SEDD provide the theoretical grounding to make meaningful progress.</p> <p>The gap with large language models remains significant, but SEDD shows the path forward: sound theory combined with careful implementation can challenge the dominance of autoregressive modeling.</p> <h2 id="references">References</h2> <p>Lou, A., Meng, C., &amp; Ermon, S. (2024). Discrete Diffusion Modeling by Estimating the Ratios of the Data Distribution. <em>Proceedings of the 41st International Conference on Machine Learning (ICML)</em>, Vienna, Austria.</p> <p>Austin, J., Johnson, D. D., Ho, J., Tarlow, D., &amp; van den Berg, R. (2021). Structured Denoising Diffusion Models in Discrete State-Spaces. <em>Advances in Neural Information Processing Systems (NeurIPS)</em>.</p> <p>Ho, J., Jain, A., &amp; Abbeel, P. (2020). Denoising Diffusion Probabilistic Models. <em>Advances in Neural Information Processing Systems (NeurIPS)</em>.</p> <p>Song, Y., &amp; Ermon, S. (2019). Generative Modeling by Estimating Gradients of the Data Distribution. <em>Advances in Neural Information Processing Systems (NeurIPS)</em>.</p> <p>Hyvärinen, A. (2005). Estimation of Non-Normalized Statistical Models by Score Matching. <em>Journal of Machine Learning Research</em>, 6, 695-709.</p> <p>Devlin, J., Chang, M. W., Lee, K., &amp; Toutanova, K. (2019). BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding. <em>Proceedings of NAACL-HLT</em>.</p> <p>Campbell, A., Benton, J., De Bortoli, V., Rainforth, T., Deligiannidis, G., &amp; Doucet, A. (2022). A Continuous Time Framework for Discrete Denoising Models. <em>Advances in Neural Information Processing Systems (NeurIPS)</em>.</p> <p>Gulrajani, I., &amp; Hashimoto, T. (2023). PLAID: An Efficient Engine for Late Interaction Document Re-ranking. <em>arXiv preprint arXiv:2301.11308</em>.</p>]]></content><author><name></name></author></entry><entry><title type="html">a distill-style blog post</title><link href="https://satgoy152.github.io/blog/2021/distill/" rel="alternate" type="text/html" title="a distill-style blog post"/><published>2021-05-22T00:00:00+00:00</published><updated>2021-05-22T00:00:00+00:00</updated><id>https://satgoy152.github.io/blog/2021/distill</id><content type="html" xml:base="https://satgoy152.github.io/blog/2021/distill/"><![CDATA[<h2 id="equations">Equations</h2> <p>This theme supports rendering beautiful math in inline and display modes using <a href="https://www.mathjax.org/">MathJax 3</a> engine. You just need to surround your math expression with <code class="language-plaintext highlighter-rouge">$$</code>, like <code class="language-plaintext highlighter-rouge">$$ E = mc^2 $$</code>. If you leave it inside a paragraph, it will produce an inline expression, just like \(E = mc^2\).</p> <p>To use display mode, again surround your expression with <code class="language-plaintext highlighter-rouge">$$</code> and place it as a separate paragraph. Here is an example:</p> \[\left( \sum_{k=1}^n a_k b_k \right)^2 \leq \left( \sum_{k=1}^n a_k^2 \right) \left( \sum_{k=1}^n b_k^2 \right)\] <p>Note that MathJax 3 is <a href="https://docs.mathjax.org/en/latest/upgrading/whats-new-3.0.html">a major re-write of MathJax</a> that brought a significant improvement to the loading and rendering speed, which is now <a href="http://www.intmath.com/cg5/katex-mathjax-comparison.php">on par with KaTeX</a>.</p> <hr/> <h2 id="citations">Citations</h2> <p>Citations are then used in the article body with the <code class="language-plaintext highlighter-rouge">&lt;d-cite&gt;</code> tag. The key attribute is a reference to the id provided in the bibliography. The key attribute can take multiple ids, separated by commas.</p> <p>The citation is presented inline like this: <d-cite key="gregor2015draw"></d-cite> (a number that displays more information on hover). If you have an appendix, a bibliography is automatically created and populated in it.</p> <p>Distill chose a numerical inline citation style to improve readability of citation dense articles and because many of the benefits of longer citations are obviated by displaying more information on hover. However, we consider it good style to mention author last names if you discuss something at length and it fits into the flow well — the authors are human and it’s nice for them to have the community associate them with their work.</p> <hr/> <h2 id="footnotes">Footnotes</h2> <p>Just wrap the text you would like to show up in a footnote in a <code class="language-plaintext highlighter-rouge">&lt;d-footnote&gt;</code> tag. The number of the footnote will be automatically generated.<d-footnote>This will become a hoverable footnote.</d-footnote></p> <hr/> <h2 id="code-blocks">Code Blocks</h2> <p>Syntax highlighting is provided within <code class="language-plaintext highlighter-rouge">&lt;d-code&gt;</code> tags. An example of inline code snippets: <code class="language-plaintext highlighter-rouge">&lt;d-code language="html"&gt;let x = 10;&lt;/d-code&gt;</code>. For larger blocks of code, add a <code class="language-plaintext highlighter-rouge">block</code> attribute:</p> <d-code block="" language="javascript"> var x = 25; function(x) { return x * x; } </d-code> <p><strong>Note:</strong> <code class="language-plaintext highlighter-rouge">&lt;d-code&gt;</code> blocks do not look good in the dark mode. You can always use the default code-highlight using the <code class="language-plaintext highlighter-rouge">highlight</code> liquid tag:</p> <figure class="highlight"><pre><code class="language-javascript" data-lang="javascript"><span class="kd">var</span> <span class="nx">x</span> <span class="o">=</span> <span class="mi">25</span><span class="p">;</span>
<span class="kd">function</span><span class="p">(</span><span class="nx">x</span><span class="p">)</span> <span class="p">{</span>
<span class="k">return</span> <span class="nx">x</span> <span class="err">\</span><span class="o">*</span> <span class="nx">x</span><span class="p">;</span>
<span class="p">}</span></code></pre></figure> <hr/> <h2 id="interactive-plots">Interactive Plots</h2> <p>You can add interative plots using plotly + iframes :framed_picture:</p> <div class="l-page"> <iframe src="/assets/plotly/demo.html" frameborder="0" scrolling="no" height="500px" width="100%" style="border: 1px dashed grey;"></iframe> </div> <p>The plot must be generated separately and saved into an HTML file. To generate the plot that you see above, you can use the following code snippet:</p> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="kn">import</span> <span class="n">pandas</span> <span class="k">as</span> <span class="n">pd</span>
<span class="kn">import</span> <span class="n">plotly.express</span> <span class="k">as</span> <span class="n">px</span>
<span class="n">df</span> <span class="o">=</span> <span class="n">pd</span><span class="p">.</span><span class="nf">read_csv</span><span class="p">(</span>
<span class="sh">'</span><span class="s">https://raw.githubusercontent.com/plotly/datasets/master/earthquakes-23k.csv</span><span class="sh">'</span>
<span class="p">)</span>
<span class="n">fig</span> <span class="o">=</span> <span class="n">px</span><span class="p">.</span><span class="nf">density_mapbox</span><span class="p">(</span>
<span class="n">df</span><span class="p">,</span>
<span class="n">lat</span><span class="o">=</span><span class="sh">'</span><span class="s">Latitude</span><span class="sh">'</span><span class="p">,</span>
<span class="n">lon</span><span class="o">=</span><span class="sh">'</span><span class="s">Longitude</span><span class="sh">'</span><span class="p">,</span>
<span class="n">z</span><span class="o">=</span><span class="sh">'</span><span class="s">Magnitude</span><span class="sh">'</span><span class="p">,</span>
<span class="n">radius</span><span class="o">=</span><span class="mi">10</span><span class="p">,</span>
<span class="n">center</span><span class="o">=</span><span class="nf">dict</span><span class="p">(</span><span class="n">lat</span><span class="o">=</span><span class="mi">0</span><span class="p">,</span> <span class="n">lon</span><span class="o">=</span><span class="mi">180</span><span class="p">),</span>
<span class="n">zoom</span><span class="o">=</span><span class="mi">0</span><span class="p">,</span>
<span class="n">mapbox_style</span><span class="o">=</span><span class="sh">"</span><span class="s">stamen-terrain</span><span class="sh">"</span><span class="p">,</span>
<span class="p">)</span>
<span class="n">fig</span><span class="p">.</span><span class="nf">show</span><span class="p">()</span>
<span class="n">fig</span><span class="p">.</span><span class="nf">write_html</span><span class="p">(</span><span class="sh">'</span><span class="s">assets/plotly/demo.html</span><span class="sh">'</span><span class="p">)</span></code></pre></figure> <hr/> <h2 id="details-boxes">Details boxes</h2> <p>Details boxes are collapsible boxes which hide additional information from the user. They can be added with the <code class="language-plaintext highlighter-rouge">details</code> liquid tag:</p> <details><summary>Click here to know more</summary> <p>Additional details, where math \(2x - 1\) and <code class="language-plaintext highlighter-rouge">code</code> is rendered correctly.</p> </details> <hr/> <h2 id="layouts">Layouts</h2> <p>The main text column is referred to as the body. It is the assumed layout of any direct descendants of the <code class="language-plaintext highlighter-rouge">d-article</code> element.</p> <div class="fake-img l-body"> <p>.l-body</p> </div> <p>For images you want to display a little larger, try <code class="language-plaintext highlighter-rouge">.l-page</code>:</p> <div class="fake-img l-page"> <p>.l-page</p> </div> <p>All of these have an outset variant if you want to poke out from the body text a little bit. For instance:</p> <div class="fake-img l-body-outset"> <p>.l-body-outset</p> </div> <div class="fake-img l-page-outset"> <p>.l-page-outset</p> </div> <p>Occasionally you’ll want to use the full browser width. For this, use <code class="language-plaintext highlighter-rouge">.l-screen</code>. You can also inset the element a little from the edge of the browser by using the inset variant.</p> <div class="fake-img l-screen"> <p>.l-screen</p> </div> <div class="fake-img l-screen-inset"> <p>.l-screen-inset</p> </div> <p>The final layout is for marginalia, asides, and footnotes. It does not interrupt the normal flow of <code class="language-plaintext highlighter-rouge">.l-body</code> sized text except on mobile screen sizes.</p> <div class="fake-img l-gutter"> <p>.l-gutter</p> </div> <hr/> <h2 id="other-typography">Other Typography?</h2> <p>Emphasis, aka italics, with <em>asterisks</em> (<code class="language-plaintext highlighter-rouge">*asterisks*</code>) or <em>underscores</em> (<code class="language-plaintext highlighter-rouge">_underscores_</code>).</p> <p>Strong emphasis, aka bold, with <strong>asterisks</strong> or <strong>underscores</strong>.</p> <p>Combined emphasis with <strong>asterisks and <em>underscores</em></strong>.</p> <p>Strikethrough uses two tildes. <del>Scratch this.</del></p> <ol> <li>First ordered list item</li> <li>Another item ⋅⋅* Unordered sub-list.</li> <li>Actual numbers don’t matter, just that it’s a number ⋅⋅1. Ordered sub-list</li> <li>And another item.</li> </ol> <p>⋅⋅⋅You can have properly indented paragraphs within list items. Notice the blank line above, and the leading spaces (at least one, but we’ll use three here to also align the raw Markdown).</p> <p>⋅⋅⋅To have a line break without a paragraph, you will need to use two trailing spaces.⋅⋅ ⋅⋅⋅Note that this line is separate, but within the same paragraph.⋅⋅ ⋅⋅⋅(This is contrary to the typical GFM line break behaviour, where trailing spaces are not required.)</p> <ul> <li> <p>Unordered list can use asterisks</p> </li> <li> <p>Or minuses</p> </li> <li> <p>Or pluses</p> </li> </ul> <p><a href="https://www.google.com">I’m an inline-style link</a></p> <p><a href="https://www.google.com" title="Google's Homepage">I’m an inline-style link with title</a></p> <p><a href="https://www.mozilla.org">I’m a reference-style link</a></p> <p><a href="http://slashdot.org">You can use numbers for reference-style link definitions</a></p> <p>Or leave it empty and use the <a href="http://www.reddit.com">link text itself</a>.</p> <p>URLs and URLs in angle brackets will automatically get turned into links. http://www.example.com or <a href="http://www.example.com">http://www.example.com</a> and sometimes example.com (but not on Github, for example).</p> <p>Some text to show that the reference links can follow later.</p> <p>Here’s our logo (hover to see the title text):</p> <p>Inline-style: <img src="https://github.com/adam-p/markdown-here/raw/master/src/common/images/icon48.png" alt="alt text" title="Logo Title Text 1"/></p> <p>Reference-style: <img src="https://github.com/adam-p/markdown-here/raw/master/src/common/images/icon48.png" alt="alt text" title="Logo Title Text 2"/></p> <p>Inline <code class="language-plaintext highlighter-rouge">code</code> has <code class="language-plaintext highlighter-rouge">back-ticks around</code> it.</p> <div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">var</span> <span class="nx">s</span> <span class="o">=</span> <span class="dl">"</span><span class="s2">JavaScript syntax highlighting</span><span class="dl">"</span><span class="p">;</span>
<span class="nf">alert</span><span class="p">(</span><span class="nx">s</span><span class="p">);</span>
</code></pre></div></div> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">s</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Python syntax highlighting</span><span class="sh">"</span>
<span class="k">print</span> <span class="n">s</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>No language indicated, so no syntax highlighting.
But let's throw in a &lt;b&gt;tag&lt;/b&gt;.
</code></pre></div></div> <p>Colons can be used to align columns.</p> <table> <thead> <tr> <th>Tables</th> <th style="text-align: center">Are</th> <th style="text-align: right">Cool</th> </tr> </thead> <tbody> <tr> <td>col 3 is</td> <td style="text-align: center">right-aligned</td> <td style="text-align: right">$1600</td> </tr> <tr> <td>col 2 is</td> <td style="text-align: center">centered</td> <td style="text-align: right">$12</td> </tr> <tr> <td>zebra stripes</td> <td style="text-align: center">are neat</td> <td style="text-align: right">$1</td> </tr> </tbody> </table> <p>There must be at least 3 dashes separating each header cell. The outer pipes (|) are optional, and you don’t need to make the raw Markdown line up prettily. You can also use inline Markdown.</p> <table> <thead> <tr> <th>Markdown</th> <th>Less</th> <th>Pretty</th> </tr> </thead> <tbody> <tr> <td><em>Still</em></td> <td><code class="language-plaintext highlighter-rouge">renders</code></td> <td><strong>nicely</strong></td> </tr> <tr> <td>1</td> <td>2</td> <td>3</td> </tr> </tbody> </table> <blockquote> <p>Blockquotes are very handy in email to emulate reply text. This line is part of the same quote.</p> </blockquote> <p>Quote break.</p> <blockquote> <p>This is a very long line that will still be quoted properly when it wraps. Oh boy let’s keep writing to make sure this is long enough to actually wrap for everyone. Oh, you can <em>put</em> <strong>Markdown</strong> into a blockquote.</p> </blockquote> <p>Here’s a line for us to start with.</p> <p>This line is separated from the one above by two newlines, so it will be a <em>separate paragraph</em>.</p> <p>This line is also a separate paragraph, but… This line is only separated by a single newline, so it’s a separate line in the <em>same paragraph</em>.</p>]]></content><author><name>Albert Einstein</name></author><category term="distill"/><category term="formatting"/><summary type="html"><![CDATA[an example of a distill-style blog post and main elements]]></summary></entry></feed>